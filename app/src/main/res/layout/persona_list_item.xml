<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

  <android.support.constraint.ConstraintLayout
      android:orientation="horizontal"
      android:layout_width="match_parent"
      android:layout_height="64dp"
      android:padding="8dp"
      android:clickable="true"
      android:background="?attr/selectableItemBackground"
      >

    <TextView
        android:id="@+id/persona_name"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Arsene"
        />
    <TextView
        android:id="@+id/persona_arcana"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:textAlignment="textEnd"
        android:textAppearance="@style/TextAppearance.AppCompat.Subhead"
        app:layout_constraintTop_toBottomOf="@+id/persona_name"
        app:layout_constraintRight_toRightOf="@id/persona_name"
        tools:text="Fool"
        />

    <TextView
        android:id="@+id/persona_level"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Small"
        android:fontFamily="sans-serif-condensed"
        tools:text="99"
        app:layout_constraintTop_toBottomOf="@id/persona_name"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBaseline_toBaselineOf="@id/persona_arcana"
        />


    <TextView
        android:id="@+id/persona_elements"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Small"
        android:typeface="monospace"
        android:ellipsize="none"
        android:maxLines="1"
        android:layout_marginLeft="16dp"
        tools:text="@string/test_colors"
        app:layout_constraintLeft_toRightOf="@+id/persona_name"
        app:layout_constraintBaseline_toBaselineOf="@id/persona_name"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/persona_stats"
        android:layout_width="0dp"
        android:layout_height="20dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Small"
        android:fontFamily="sans-serif-condensed"
        android:typeface="monospace"
        android:layout_marginLeft="16dp"
        android:ellipsize="none"
        android:maxLines="1"
        app:layout_constraintLeft_toRightOf="@id/persona_arcana"
        app:layout_constraintTop_toBottomOf="@id/persona_elements"
        app:layout_constraintBaseline_toBaselineOf="@id/persona_arcana"
        tools:text="@string/test_stats"
        />

  </android.support.constraint.ConstraintLayout>
</layout>